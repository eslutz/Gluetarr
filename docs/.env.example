# Forwardarr Configuration

# Path to the port file generated by Gluetun
GLUETUN_PORT_FILE=/tmp/gluetun/forwarded_port

# Torrent Client Connection Details
TORRENT_CLIENT_URL=http://localhost:8080
TORRENT_CLIENT_USER=admin
TORRENT_CLIENT_PASSWORD=adminadmin

# Sync Settings
# How often to force check even if no file change detected (seconds)
# Set to 0 to disable periodic sync and rely only on file change events
SYNC_INTERVAL=300

# Server Settings
METRICS_PORT=9090
LOG_LEVEL=info

# Webhook Settings (optional)
# Leave WEBHOOK_URL empty to disable webhook notifications
# WEBHOOK_URL=http://your-server.com/webhook
# WEBHOOK_TIMEOUT=10
